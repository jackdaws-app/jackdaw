// Convex deployment URL. For local development this is the anonymous local
// deployment started by `npx convex dev`; swap for your cloud deployment URL
// (https://<name>.convex.cloud) before distributing the extension.
export const CONVEX_URL = "http://127.0.0.1:3210";
